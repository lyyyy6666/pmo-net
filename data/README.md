# Data

Raw datasets and processed tensors are not included in this release package.

Place the required files under this directory using the paths shown in `configs/` and `scripts/`.
The expected inputs include processed dynamic tensors or CSV files, station metadata, graph files, and meteorology tensors needed by the wind-aware physical branch.

Large raw data files, intermediate tensors, checkpoints, and prediction arrays were intentionally excluded from the release copy.
